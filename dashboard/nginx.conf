server {
  listen 80;
  server_name _;
  root /usr/share/nginx/html;
  location /api/ { proxy_pass http://control-plane:8080/api/; proxy_set_header Host $host; }
  location /healthz { proxy_pass http://control-plane:8080/healthz; }
  location / { try_files $uri /index.html; }
}

